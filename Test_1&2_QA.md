### Test No - 1

```markdown
1. What sciences are related to machine learning?  
  ( С какими науками связано машинное обучение? )

- ✅ Mathematical statistics (Математическая статистика)  
- ✅ Pattern recognition (Распознавание образов)  
- ✅ Neural network theory (Теория нейронных сетей)  
- ❌ Neurobiology (Нейробиология)  
---

2. Which of the following statements are true?  
  ( Какие из приведенных утверждений являются верными? )

- ✅ Deep learning is a subset of machine learning (Глубокое обучение – подраздел машинного обучения)  
- ❌ Machine learning is based on formalizing expert knowledge to build models (Машинное обучение основано на формализации знаний экспертов для построения моделей)  
- ❌ Knowledge base is a key tool in machine learning (База знаний – ключевой инструмент машинного обучения)  
- ✅ The tasks of machine learning and mathematical statistics overlap to a great extent (Задачи машинного обучения и математической статистики во многом пересекаются)  
---

3. What approach is best for modeling an object or process described by a linear differential equation?  
  ( Какой подход целесообразно использовать для моделирования объекта или процесса, описываемого линейным дифференциальным уравнением? )

- ❌ Data-driven (Data-driven)  
- ✅ Model-based (Model-based)  
---

4. What are the features of the data-driven approach to building models?  
  ( Каковы особенности data-driven подхода к построению моделей? )

- ✅ The model is built based on training data (Модель строится на основе обучающих данных)  
- ❌ A database is necessary for building the model (Для построения модели необходима база данных)  
- ❌ The model receives some data as input (Модель получает на входе некоторые данные)  
- ❌ The model is integrated into a data processing system (Модель интегрирована в некоторую систему обработки данных)  
---

5. In which case is it appropriate to use the data-driven approach to build an object model?  
  ( В каком случае целесообразно использование data-driven подхода для построения модели объекта? )

- ❌ If there is enough data about the object's operation and the object is simple (Если имеется достаточно данных о функционировании объекта и объект простой)  
- ✅ If there is enough data about the object's operation and the object is complex (Если имеется достаточно данных о функционировании объекта и объект сложный)  
- ❌ Data-driven approach should be used in all cases because it is more accurate (В любом случае нужно использовать data-driven подход, т.к. он более точный)  
- ❌ If the model is to be integrated into a data processing system (Если предполагается интегрировать модель в некоторую систему обработки данных)  
---

6. What are the disadvantages of the data-driven approach compared to the model-based approach?  
  ( В чем недостатки data-driven подхода по сравнению с model-based подходом? )

- ✅ It is a more labor-intensive process to build the model (Более трудоемкий процесс построения модели)  
- ✅ The model is typically difficult to interpret (Модель, как правило, сложно интерпретируемая)  
- ✅ The model usually has higher computational complexity (Модель, как правило, имеет большую вычислительную сложность)  
- ✅ Special methods for accuracy estimation are required for the model (Требуются специальные методы оценки точности модели)  
---

7. What are the differences between machine learning and mathematical statistics?  
  ( В чем отличия машинного обучения от математической статистики? )

- ❌ There are no differences; they are the same (Отличий нет, это одно и то же)  
- ✅ In mathematical statistics, models are typically more interpretable (В математической статистике, как правило, рассматриваются более интерпретируемые модели)  
- ✅ In machine learning, model parameters are obtained through iterative training, while in mathematical statistics they are obtained through analytical calculations (В машинном обучении, как правило, параметры моделей получаются в результате итерационной процедуры обучения, а в математической статистике – в результате аналитических расчетов)  
- ❌ A database is necessary for building machine learning models (Для построения моделей машинного обучения необходима база данных)  
---

8. What stages are involved in the general scheme of solving machine learning problems?  
  ( Какие этапы входят в общую схему решения задач машинного обучения? )

- ✅ Feature extraction (Выделение признаков)  
- ✅ Evaluation of model accuracy (Оценка точности обученной модели)  
- ❌ Data labeling (Разметка данных)  
- ❌ Rule formalization (Формализация правил)  
---

9. What tasks does supervised machine learning solve?  
  ( Какие задачи решает машинное обучение с учителем? )

- ✅ Data classification (Классификация данных)  
- ❌ Data clustering (Кластеризация данных)  
- ✅ Regression (Регрессия)  
- ❌ Anomaly detection (Обнаружение аномалий)  
---

10. What tasks does unsupervised machine learning solve?  
   ( Какие задачи решает машинное обучение без учителя? )

- ❌ Data classification (Классификация данных)  
- ✅ Data clustering (Кластеризация данных)  
- ❌ Regression (Регрессия)  
- ✅ Anomaly detection (Обнаружение аномалий)  
---

11. What type of machine learning task does face recognition on an image belong to?  
   ( К какому типу задач машинного обучения относится задача распознавания лиц на изображении? )

- ❌ Regression (Регрессия)  
- ✅ Classification (Классификация)  
- ❌ Clustering (Кластеризация)  
- ❌ Optimization (Оптимизация)  
---

12. Which of the following is not part of machine learning?  
   ( Что из приведенного не является частью машинного обучения? )
 
- ❌ Supervised learning (Обучение с учителем)  
- ❌ Self-learning (Самообучение)  
- ❌ Reinforcement learning (Обучение с подкреплением)  
- ❌ Active learning (Активное обучение)  
- ✅ None of the above options are correct (Среди приведенных вариантов нет правильного)  
---

13. Which of the following methods relate to supervised learning?  
   ( Какие из приведенных методов относятся к обучению с учителем? )
 
- ✅ Linear regression (Линейная регрессия)  
- ✅ Logistic regression (Логистическая регрессия)  
- ❌ K-means method (Метод K-средних)  
- ❌ Principal component analysis (Метод главных компонентов)  
---

14. Which of the following models do not belong to supervised learning models?  
   ( Какие из приведенных моделей не относятся к моделям обучения с учителем? )
 
- ❌ Neural networks (Нейронные сети)  
- ❌ Decision trees (Деревья решений)  
- ✅ Association rules (Ассоциативные правила)  
- ✅ Self-organizing maps (Самообучающиеся карты)  
---

15. What is a labeled dataset?  
   ( Что такое размеченная выборка? )
 
- ❌ A dataset that has labels (метки) (Выборка, имеющая название (метку))  
- ❌ A dataset divided into training, validation, and test sets (Выборка, разделенная на обучающую, валидационную и тестовую части)  
- ❌ A dataset stored in a database table where each attribute has its label (Выборка, хранящаяся в таблице базы данных, где каждый атрибут имеет свою метку)  
- ❌ A dataset where elements are ordered (Выборка, элементы которой упорядочены)  
- ✅ None of the above options are correct (Среди приведенных вариантов нет правильного)  
---

16. Which of the following statements are true for supervised learning?  
   ( Какие из приведенных утверждений верны для обучения с учителем? )
 
- ✅ Each training example contains the desired output value (label) or a vector of values (Каждый обучающий пример содержит желаемое выходное значение (метку) или вектор значений)  
- ❌ Each training example contains only the input value or a vector of values (Каждый обучающий пример содержит только входное значение или вектор значений)  
- ❌ Training examples may or may not contain the desired output value (label) or a vector of values (Обучающие примеры могут как содержать желаемое выходное значение (метку) или вектор значений, так и не содержать)  
- ❌ If the training example contains a label, it must be represented by an integer (Если обучающий пример содержит метку, то она должна быть представлена целым числом)  
---

17. Which of the following statements are true for unsupervised learning?  
   ( Какие из приведенных утверждений верны для обучения без учителя? )
 
- ❌ Each training example contains the desired output value (label) or a vector of values (Каждый обучающий пример содержит желаемое выходное значение (метку) или вектор значений)  
- ✅ Each training example contains only the input value or a vector of values (Каждый обучающий пример содержит только входное значение или вектор значений)  
- ❌ Training examples may or may not contain the desired output value (label) or a vector of values (Обучающие примеры могут как содержать желаемое выходное значение (метку) или вектор значений, так и не содержать)  
- ❌ If the training example contains a label, it must be represented by an integer (Если обучающий пример содержит метку, то она должна быть представлена целым числом)  
---

18. Which of the following statements are true for semi-supervised learning?  
   ( Какие из приведенных утверждений верны для обучения с частичным привлечением учителя? )
 
- ❌ Each training example contains the desired output value (label) or a vector of values (Каждый обучающий пример содержит желаемое выходное значение (метку) или вектор значений)  
- ❌ Each training example contains only the input value or a vector of values (Каждый обучающий пример содержит только входное значение или вектор значений)  
- ✅ Training examples may or may not contain the desired output value (label) or a vector of values (Обучающие примеры могут как содержать желаемое выходное значение (метку) или вектор значений, так и не содержать)  
- ❌ If the training example contains a label, it must be represented by an integer (Если обучающий пример содержит метку, то она должна быть представлена целым числом)  
---

19. What tasks does unsupervised machine learning solve?  
   ( Какие задачи решает машинное обучение без учителя? )
 
- ❌ Function approximation (Аппроксимация функций)  
- ❌ Time series forecasting (Прогноз временных рядов)  
- ✅ Anomaly detection (Обнаружение аномалий в данных)  
- ✅ Dimensionality reduction (Снижение размерности данных)  
---

20. What tasks does supervised machine learning solve?  
   ( Какие задачи решает машинное обучение с учителем? )
 
- ✅ Function approximation (Аппроксимация функций)  
- ✅ Time series forecasting (Прогноз временных рядов)  
- ❌ Anomaly detection (Обнаружение аномалий в данных)  
- ❌ Dimensionality reduction (Снижение размерности данных)  
---

21. What tasks does semi-supervised machine learning solve?  
   ( Какие задачи решает машинное обучение с частичным привлечением учителя? )
 
- ✅ Data classification (Классификация данных)  
- ✅ Data clustering (Кластеризация данных)  
- ❌ Robot navigation (Навигация роботов)  
- ❌ Function optimization (Оптимизация функций)  
---

22. What is an ensemble of machine learning models?  
   ( Что такое ансамбль моделей машинного обучения? )

- ❌ A set of similar machine learning models, each solving a separate task (Совокупность однотипных моделей машинного обучения, каждая из которых решает отдельную задачу)  
- ✅ A set of machine learning models and a decision rule that aggregates their outputs (Совокупность моделей машинного обучения и решающего правила, агрегирующего их выходы)  
- ❌ A set of machine learning models solving the same task (Совокупность моделей машинного обучения, решающих одну и ту же задачу)  
- ❌ A set of machine learning models trained on different subsets of the original training dataset (Совокупность моделей машинного обучения, обученных на разных подвыборках исходной обучающей выборки)  
---
```
